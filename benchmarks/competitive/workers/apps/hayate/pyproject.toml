[project]
name = "hayate-workers-benchmark-app"
version = "0"
requires-python = ">=3.12"
dependencies = [
  "hayate==0.0.0",
  "uts46==0.2.0",
  "workers-runtime-sdk==1.6.3",
]

[tool.uv]
package = false
