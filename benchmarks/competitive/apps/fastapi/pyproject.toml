[project]
name = "fastapi-benchmark-app"
version = "0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.140.0",
  "uvicorn==0.51.0",
]

[project.optional-dependencies]
upload = ["python-multipart==0.0.32"]

[tool.uv]
package = false
