[project]
name = "hayate-benchmark-app"
version = "0"
requires-python = ">=3.12"
dependencies = [
  "hayate",
  "uvicorn==0.51.0",
]

[tool.uv]
package = false

[tool.uv.sources]
hayate = { path = "../../../..", editable = false }
