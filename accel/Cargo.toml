[package]
name = "hayate-accel"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[lib]
name = "hayate_accel"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["abi3-py312"] }

[profile.release]
lto = true
